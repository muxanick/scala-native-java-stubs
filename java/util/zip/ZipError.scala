package java.util.zip

import java.lang.{Error, InternalError, Object, Throwable, VirtualMachineError}

/** Signals that an unrecoverable error has occurred. */
class ZipError extends InternalError {
}
