package javax.xml.bind.annotation

import java.lang.Object

// Used in XmlSchemaType.type() to
// signal that the type be inferred from the signature
// of the property.
object final XmlSchemaType.DEFAULT extends Object {
